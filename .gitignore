node_modules/
dist/
.env
*.log
.DS_Store

# DevTools Local Overrides (T8.2, ручная проверка Mini App без Telegram) —
# личный dev-артефакт браузера, не часть проекта
/FolderForOverrides/

/src/generated/prisma

# AI-agent reference docs auto-installed by `prisma init` (Prisma 7) —
# duplicated 3x across tools, not needed in version control
/.agents/
/.claude/
/.windsurf/
/skills-lock.json
